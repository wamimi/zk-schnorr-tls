[package]
name = "simulator"
version = "0.1.0"
edition = "2024"

[dependencies]
zk_schnorr_lib = { path = "../zk_schnorr_lib" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
clap = { version = "4.4", features = ["derive"] }
